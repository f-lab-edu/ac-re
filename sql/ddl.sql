CREATE TABLE MEMBER (
    rownum INT AUTO_INCREMENT PRIMARY KEY,
    id VARCHAR(50),
    password VARCHAR(100),
    name VARCHAR(50),
    nickname VARCHAR(100),
    email VARCHAR(200),
    phone VARCHAR(20)
);
